# 用户认证系统测试指南

## ✅ 已完成功能

### 1. 用户注册
- ✅ 注册页面 (`/register`)
- ✅ 邮箱验证
- ✅ 用户名设置
- ✅ 密码强度验证（至少6位）
- ✅ 自动创建用户记录

### 2. 用户登录
- ✅ 登录页面 (`/login`)
- ✅ 邮箱密码登录
- ✅ 错误提示
- ✅ 登录后跳转到 Dashboard

### 3. Dashboard
- ✅ 用户信息显示
- ✅ 退出登录功能
- ✅ 基础统计卡片
- ✅ 路由保护（未登录自动跳转）

---

## 🧪 测试步骤

### 测试 1：注册新用户

1. 打开浏览器访问：http://localhost:3000
2. 点击 "注册" 按钮
3. 填写信息：
   - 用户名：`测试用户`
   - 邮箱：`test@example.com`
   - 密码：`123456`
4. 点击 "注册"
5. **预期结果**：
   - 显示 "注册成功！请查收邮箱验证邮件。"
   - 自动跳转到登录页面

**注意**：Supabase 默认需要邮箱验证。如果你想跳过邮箱验证，需要在 Supabase 设置中关闭。

### 关闭邮箱验证（可选）

1. 进入 Supabase 项目
2. 点击左侧 **Authentication**
3. 点击 **Providers**
4. 找到 **Email** 提供商
5. 关闭 **Confirm email** 选项
6. 保存

### 测试 2：用户登录

1. 访问：http://localhost:3000/login
2. 输入刚才注册的邮箱和密码
3. 点击 "登录"
4. **预期结果**：
   - 成功登录
   - 跳转到 Dashboard (`/dashboard`)
   - 显示用户名和统计信息

### 测试 3：路由保护

1. 退出登录
2. 直接访问：http://localhost:3000/dashboard
3. **预期结果**：
   - 自动跳转到登录页面

### 测试 4：退出登录

1. 在 Dashboard 页面
2. 点击右上角 "退出登录"
3. **预期结果**：
   - 成功退出
   - 跳转到登录页面

---

## 🎨 设计风格

### 简约高级感设计

**配色方案**：
- 主色：绿色 `#7cb342`（成长、活力）
- 辅色：橙色 `#ffb74d`（专注）
- 背景：渐变 `from-green-50 to-blue-50`
- 卡片：白色 + 细边框

**设计特点**：
- ✨ 极简布局，大量留白
- ✨ 微圆角（8px）
- ✨ 精致的阴影和边框
- ✨ 流畅的过渡动画
- ✨ 清晰的视觉层级

---

## 📊 数据库验证

### 检查用户是否创建成功

1. 进入 Supabase 项目
2. 点击左侧 **Table Editor**
3. 选择 `users` 表
4. **预期结果**：
   - 看到新注册的用户记录
   - 包含：id, email, username, level=1, exp=0

### 检查认证用户

1. 点击左侧 **Authentication**
2. 点击 **Users**
3. **预期结果**：
   - 看到注册的用户
   - 显示邮箱和创建时间

---

## 🐛 常见问题

### Q1: 注册后提示 "注册失败"
**A**: 检查：
- Supabase URL 和 Key 是否正确配置
- 网络连接是否正常
- 查看浏览器控制台的错误信息

### Q2: 登录后没有跳转到 Dashboard
**A**: 检查：
- 浏览器控制台是否有错误
- 刷新页面重试
- 清除浏览器缓存

### Q3: 邮箱验证邮件没收到
**A**: 
- 检查垃圾邮件文件夹
- 或者在 Supabase 中关闭邮箱验证（见上文）

### Q4: Dashboard 显示 "加载中..." 不停
**A**: 检查：
- 用户是否已登录
- Supabase 连接是否正常
- 浏览器控制台错误信息

---

## 🎯 下一步开发

用户认证系统已完成！接下来可以开发：

1. **任务管理功能** ⏭️
   - 创建、编辑、删除任务
   - 任务列表展示
   - 时间块视图

2. **番茄钟功能**
   - 计时器
   - 白噪音播放
   - 全屏模式

3. **习惯养成功能**
   - 习惯创建
   - 打卡功能
   - 日历视图

---

## 📝 技术细节

### 认证流程

```
注册流程：
用户填写表单 → Supabase Auth 创建用户 → 触发器自动创建 users 记录 → 发送验证邮件

登录流程：
用户输入邮箱密码 → Supabase Auth 验证 → 返回 session → 存储到浏览器 → 跳转到 Dashboard

路由保护：
访问 Dashboard → 检查 session → 如果未登录 → 跳转到登录页
```

### 使用的技术

- **Supabase Auth**: 用户认证和会话管理
- **Next.js App Router**: 路由和页面管理
- **React Hooks**: 状态管理
- **Tailwind CSS**: 样式设计

---

## ✨ 设计亮点

1. **简约优雅**：去除多余装饰，专注内容
2. **流畅动画**：hover 和 focus 状态的过渡效果
3. **清晰反馈**：错误提示、加载状态、成功消息
4. **响应式设计**：适配桌面和移动端
5. **无障碍支持**：语义化 HTML、键盘导航

---

现在可以开始测试了！🚀
